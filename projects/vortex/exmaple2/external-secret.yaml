fffapiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: example-2
spec:
  dataFrom:
  - find:
      path: /example-2/stage/
      name:
        regexp: '.*'
    rewrite:
    - regexp:
        source: "/example-2/stage/(.*)"
        target: "$1"
